PROJECT(EPANET C)

INCLUDE_DIRECTORIES(./
./Include)

FILE(GLOB EPANET_SRCS "./Source/*.c")
set_source_files_properties(${EPANET_SRCS} PROPERTIES LANGUAGE C)

ADD_LIBRARY(EPANET STATIC ${EPANET_SRCS})

